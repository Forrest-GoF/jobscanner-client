import Layout from '@/components/layout';
import { JobList } from '@/components/features';

const JobSearch = () => {
	return (
		<Layout>
			<JobList />
		</Layout>
	);
};

export default JobSearch;
